# No trading keys needed for MVP (read-only public Binance REST)
# Optional LLM (bounded review layer) — leave empty to disable LLM and default to NO_TRADE-safe degradation
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini
